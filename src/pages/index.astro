---
import PortfolioGrid from '../components/PortfolioGrid.jsx';
import Main from '../layouts/main.astro';

const portfolio = (await Astro.glob('./portfolio/**/*.md')).map((item) => {
	return {...item.frontmatter, href: item.url}
}).reverse()
---

<Main title="Home" description="Home of Ethan Ou">
	<div class="flex items-center main-container h-screen-60">
		<div class="max-w-xl">
			<h1 class="font-bold text-4xl">Hey, I’m Ethan - Media and Technology from Sydney.</h1>
			<p class="pt-6 text-lg">Currently working in Education.</p>
		</div>
	</div>
	<div class="main-container">
		<h2 class="font-bold text-3xl">Portfolio</h2>
        <PortfolioGrid client:load items={portfolio} />
	</div>
</Main>